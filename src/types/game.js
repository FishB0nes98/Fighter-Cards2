// This file is just for documentation since we're using plain JavaScript
// The types are kept as comments for reference

/*
export type CardType = 'MINION' | 'SPELL';

export interface BaseCard {
  id: string;
  name: string;
  manaCost: number;
  type: CardType;
  description: string;
  imageUrl?: string;
}

export interface MinionCard extends BaseCard {
  type: 'MINION';
  attack: number;
  health: number;
  canAttackImmediately?: boolean;
  hasAttackedThisTurn?: boolean;
  isNewlyPlayed?: boolean;
  battlecry?: (gameState: GameState) => GameState;
  animation?: {
    isAttacking?: boolean;
    isDying?: boolean;
    isHealing?: boolean;
    targetIndex?: number;
  };
}

export interface SpellCard extends BaseCard {
  type: 'SPELL';
  effect: (gameState: GameState, targetInfo?: any) => GameState;
  animation?: {
    isPlaying?: boolean;
    targetIndex?: number;
  };
}
*/

// No actual code needed here since we're using plain JavaScript
// This file is just for documentation 